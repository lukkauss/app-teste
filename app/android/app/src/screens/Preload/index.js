import react from 'react';
import {text} from 'react-native';
import { container } from './styles';

export default () => {
    return(
        <container>
            <text> Preload</text>
        </container>
    )
}